{
  "name": "mcsquishy",
  "version": "0.1.0",
  "private": true,
  "description": "McSquishy: Blob on the Run",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run && node --experimental-strip-types --test src/input/keyboard-input.test.ts src/game/stateMachine.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "5.9.2",
    "vite": "7.1.5",
    "vitest": "3.2.4"
  }
}
